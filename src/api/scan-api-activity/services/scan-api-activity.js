'use strict';

/**
 * scan-api-activity service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::scan-api-activity.scan-api-activity');
